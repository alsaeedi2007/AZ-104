## AZ-104 Prerequistses 

1. Describe cloud shell and it's functionality
2. Deteremine whether cloud shell meets your organization needs 
3. Recognize How to use cloud shell ?

## Introduction to Cloud Shell 

1. `Cloud shell` versus `Powershell` vs `AZURE CLI`, As per info and understanding cloud has a `shell` this shell called `AZURE CLI` as the name cloud implied vendor agnostic while in our case of AZURE, we named it AZURE CLI however this `AZURE CLI` has either `BASH` or `Powershell` syntax 
2. Azure Cloud Shell also provides cloud storage to persist files such as SSH keys, scripts, and more.
3.  you have access to a command-line environment fully connected with your account's permissions and the resources to which you have access. All that works in an infrastructure that's compliant with double encryption at rest by default. You don't need to take any further action.

## Cloud Shell toolbox 
Cloud shell has many tools that includes but not limited to:
1. 

